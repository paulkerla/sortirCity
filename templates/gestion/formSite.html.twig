{% extends 'base.html.twig' %}

{% block title %} Site {% endblock %}

{% block body %}

    <h1 class="display-4 text-dark">Add Site</h1>
    {{ form_start(form) }}
    {{ form_row(form.name) }}
    <button type="submit" class="btn btn-primary">Submit</button>
    {{ form_end(form) }}


{% endblock %}
