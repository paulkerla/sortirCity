{% extends 'base.html.twig' %}

{% block title %} City {% endblock %}

{% block body %}

    <h1 class="display-4 text-site-color">Add city</h1>
    {{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.zipcode) }}
    <button type="submit" class="btn btn-secondary-custom">Submit</button>
    {{ form_end(form) }}


{% endblock %}
